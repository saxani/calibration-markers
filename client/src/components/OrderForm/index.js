import Input from './Input';

const OrderForm = () => {
  return (
    <form>
      A simple order form
      <Input name='Full Name' />
      <Input name='Title' />
    </form>
  );
};

export default OrderForm;
